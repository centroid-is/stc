---
phase: 01-project-bootstrap-parser
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/ast/node.go
  - pkg/ast/decl.go
  - pkg/ast/stmt.go
  - pkg/ast/expr.go
  - pkg/ast/types.go
  - pkg/ast/var.go
  - pkg/ast/trivia.go
  - pkg/ast/visitor.go
  - pkg/ast/json.go
  - pkg/ast/json_test.go
autonomous: true
requirements:
  - PARS-01
  - PARS-02
  - PARS-03
  - PARS-04
  - PARS-08
  - PARS-09

must_haves:
  truths:
    - "All IEC 61131-3 Ed.3 POU types have corresponding AST node types"
    - "CODESYS OOP constructs (METHOD, INTERFACE, PROPERTY, EXTENDS, IMPLEMENTS) have AST nodes"
    - "Every AST node carries trivia (whitespace, comments) for CST fidelity"
    - "AST nodes marshal to JSON and back without data loss"
    - "All VAR section kinds have AST representation"
    - "Pointer, Reference, Array, Struct, Enum, Subrange type specifiers exist"
  artifacts:
    - path: "pkg/ast/node.go"
      provides: "Base Node interface and NodeKind enum"
      exports: ["Node", "NodeKind", "NodeBase"]
    - path: "pkg/ast/decl.go"
      provides: "POU declaration nodes"
      exports: ["SourceFile", "ProgramDecl", "FunctionBlockDecl", "FunctionDecl", "InterfaceDecl", "MethodDecl", "PropertyDecl", "TypeDecl"]
    - path: "pkg/ast/expr.go"
      provides: "Expression nodes"
      exports: ["BinaryExpr", "UnaryExpr", "Literal", "Ident", "CallExpr", "MemberAccessExpr", "IndexExpr", "DerefExpr"]
    - path: "pkg/ast/stmt.go"
      provides: "Statement nodes"
      exports: ["IfStmt", "CaseStmt", "ForStmt", "WhileStmt", "RepeatStmt", "AssignStmt", "CallStmt", "ReturnStmt", "ExitStmt"]
    - path: "pkg/ast/types.go"
      provides: "Type specifier nodes"
      exports: ["NamedType", "ArrayType", "PointerType", "ReferenceType", "StringType", "SubrangeType", "EnumType", "StructType"]
  key_links:
    - from: "pkg/ast/node.go"
      to: "pkg/ast/trivia.go"
      via: "NodeBase embeds LeadingTrivia/TrailingTrivia"
      pattern: "LeadingTrivia.*\\[\\]Trivia"
    - from: "pkg/ast/json.go"
      to: "pkg/ast/decl.go"
      via: "JSON marshaling for all declaration types"
      pattern: "MarshalJSON"
---

<objective>
Define all AST/CST node types for the IEC 61131-3 Structured Text parser. This is the contract that the parser (Plan 04) produces and the CLI (Plan 05) consumes.

Purpose: Establish the complete type system for the concrete syntax tree before the parser or CLI are built, so both can build against stable interfaces.
Output: Complete pkg/ast/ package with all node types, JSON marshaling, visitor interface, and trivia support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-bootstrap-parser/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AST node base, trivia, and visitor infrastructure</name>
  <files>pkg/ast/node.go, pkg/ast/trivia.go, pkg/ast/visitor.go</files>
  <read_first>
    - .planning/phases/01-project-bootstrap-parser/01-RESEARCH.md (Pattern 1: CST-First with Trivia, AST node examples, NodeBase definition)
  </read_first>
  <action>
1. Create `pkg/ast/node.go`:
   - Package `ast`
   - `type NodeKind int` with iota constants for ALL node types. Group them:
     - File: `KindSourceFile`
     - Declarations: `KindProgramDecl`, `KindFunctionBlockDecl`, `KindFunctionDecl`, `KindInterfaceDecl`, `KindMethodDecl`, `KindPropertyDecl`, `KindTypeDecl`, `KindActionDecl`
     - Statements: `KindAssignStmt`, `KindCallStmt`, `KindIfStmt`, `KindCaseStmt`, `KindForStmt`, `KindWhileStmt`, `KindRepeatStmt`, `KindReturnStmt`, `KindExitStmt`, `KindContinueStmt`, `KindEmptyStmt`, `KindErrorNode`
     - Expressions: `KindBinaryExpr`, `KindUnaryExpr`, `KindLiteral`, `KindIdent`, `KindCallExpr`, `KindMemberAccessExpr`, `KindIndexExpr`, `KindDerefExpr`, `KindParenExpr`
     - Types: `KindNamedType`, `KindArrayType`, `KindPointerType`, `KindReferenceType`, `KindStringType`, `KindSubrangeType`, `KindEnumType`, `KindStructType`
     - Var: `KindVarBlock`, `KindVarDecl`
   - `func (k NodeKind) String() string` with descriptive names
   - `type Node interface { Kind() NodeKind; Span() Span; Children() []Node }` — NOTE: Span type will be defined locally until source package is wired in Plan 03
   - Define `Span` and `Pos` types locally in ast package (mirrors source.Pos/Span but avoids circular imports):
     ```go
     type Pos struct {
         File   string `json:"file"`
         Line   int    `json:"line"`
         Col    int    `json:"col"`
         Offset int    `json:"offset"`
     }
     type Span struct {
         Start Pos `json:"start"`
         End   Pos `json:"end"`
     }
     func SpanFrom(start, end Pos) Span { return Span{Start: start, End: end} }
     ```
   - `type NodeBase struct`:
     ```go
     type NodeBase struct {
         NodeKind       NodeKind  `json:"kind"`
         NodeSpan       Span      `json:"span"`
         LeadingTrivia  []Trivia  `json:"leading_trivia,omitempty"`
         TrailingTrivia []Trivia  `json:"trailing_trivia,omitempty"`
     }
     func (n *NodeBase) Kind() NodeKind { return n.NodeKind }
     func (n *NodeBase) Span() Span     { return n.NodeSpan }
     ```
   - Marker interfaces: `type Declaration interface { Node; declNode() }`, `type Statement interface { Node; stmtNode() }`, `type Expr interface { Node; exprNode() }`, `type TypeSpec interface { Node; typeSpecNode() }`
   - `type ErrorNode struct { NodeBase; Message string }` with Children() returning empty slice
   - `type Ident struct { NodeBase; Name string }` — used throughout as name references

2. Create `pkg/ast/trivia.go`:
   - `type TriviaKind int` with constants: `TriviaWhitespace`, `TriviaLineComment`, `TriviaBlockComment`
   - `func (k TriviaKind) String() string`
   - `type Trivia struct`:
     ```go
     type Trivia struct {
         Kind TriviaKind `json:"kind"`
         Text string     `json:"text"`
         Span Span       `json:"span"`
     }
     ```

3. Create `pkg/ast/visitor.go`:
   - `type Visitor interface`:
     ```go
     type Visitor interface {
         Visit(node Node) Visitor  // return nil to stop descending
     }
     ```
   - `func Walk(v Visitor, node Node)` — calls v.Visit(node), if non-nil Visitor returned, walks children recursively
   - `func Inspect(node Node, f func(Node) bool)` — convenience wrapper that calls f for each node; if f returns true, descend into children
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go build ./pkg/ast/... 2>&1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - pkg/ast/node.go contains `type NodeKind int`
    - pkg/ast/node.go contains `KindSourceFile`
    - pkg/ast/node.go contains `KindProgramDecl`
    - pkg/ast/node.go contains `KindFunctionBlockDecl`
    - pkg/ast/node.go contains `KindErrorNode`
    - pkg/ast/node.go contains `type Node interface`
    - pkg/ast/node.go contains `type NodeBase struct`
    - pkg/ast/node.go contains `type Declaration interface`
    - pkg/ast/node.go contains `type Statement interface`
    - pkg/ast/node.go contains `type Expr interface`
    - pkg/ast/node.go contains `type TypeSpec interface`
    - pkg/ast/node.go contains `type Ident struct`
    - pkg/ast/trivia.go contains `type TriviaKind int`
    - pkg/ast/trivia.go contains `type Trivia struct`
    - pkg/ast/trivia.go contains `TriviaLineComment`
    - pkg/ast/trivia.go contains `TriviaBlockComment`
    - pkg/ast/visitor.go contains `type Visitor interface`
    - pkg/ast/visitor.go contains `func Walk`
    - pkg/ast/visitor.go contains `func Inspect`
    - `go build ./pkg/ast/...` exits 0
  </acceptance_criteria>
  <done>AST base infrastructure compiles: NodeKind enum, Node interface, NodeBase with trivia, Visitor/Walk/Inspect, ErrorNode, Ident</done>
</task>

<task type="auto">
  <name>Task 2: Create all declaration, statement, expression, type spec, and var nodes with JSON marshaling and tests</name>
  <files>pkg/ast/decl.go, pkg/ast/stmt.go, pkg/ast/expr.go, pkg/ast/types.go, pkg/ast/var.go, pkg/ast/json.go, pkg/ast/json_test.go</files>
  <read_first>
    - pkg/ast/node.go (base types from Task 1)
    - pkg/ast/trivia.go (Trivia type from Task 1)
    - .planning/phases/01-project-bootstrap-parser/01-RESEARCH.md (AST Declaration Nodes example, full construct list)
  </read_first>
  <action>
1. Create `pkg/ast/decl.go` — ALL declaration node types:
   - `type SourceFile struct { NodeBase; Declarations []Declaration }` — root node
   - `type ProgramDecl struct { NodeBase; Name *Ident; VarBlocks []*VarBlock; Body []Statement }` — per PARS-01
   - `type FunctionBlockDecl struct { NodeBase; Name *Ident; Extends *Ident; Implements []*Ident; VarBlocks []*VarBlock; Body []Statement; Methods []*MethodDecl; Properties []*PropertyDecl }` — per PARS-01, PARS-02
   - `type FunctionDecl struct { NodeBase; Name *Ident; ReturnType TypeSpec; VarBlocks []*VarBlock; Body []Statement }` — per PARS-01
   - `type InterfaceDecl struct { NodeBase; Name *Ident; Extends []*Ident; Methods []*MethodSignature; Properties []*PropertySignature }` — per PARS-02
   - `type MethodDecl struct { NodeBase; AccessModifier AccessModifier; Name *Ident; ReturnType TypeSpec; VarBlocks []*VarBlock; Body []Statement; IsAbstract bool; IsFinal bool; IsOverride bool }` — per PARS-02
   - `type PropertyDecl struct { NodeBase; AccessModifier AccessModifier; Name *Ident; Type TypeSpec; Getter *MethodDecl; Setter *MethodDecl }` — per PARS-02
   - `type MethodSignature struct { NodeBase; Name *Ident; ReturnType TypeSpec; VarBlocks []*VarBlock }` — interface method signature
   - `type PropertySignature struct { NodeBase; Name *Ident; Type TypeSpec }` — interface property signature
   - `type TypeDecl struct { NodeBase; Name *Ident; Type TypeSpec }` — per PARS-01 (TYPE...END_TYPE)
   - `type ActionDecl struct { NodeBase; Name *Ident; Body []Statement }` — ACTION blocks
   - `type AccessModifier int` with constants: `AccessNone`, `AccessPublic`, `AccessPrivate`, `AccessProtected`, `AccessInternal`
   - All embed `NodeBase` and implement `Declaration` interface (declNode() method)
   - All implement `Children() []Node` returning child nodes

2. Create `pkg/ast/stmt.go` — ALL statement node types:
   - `type AssignStmt struct { NodeBase; Target Expr; Value Expr }` — `:=` assignment
   - `type CallStmt struct { NodeBase; Callee Expr; Args []*CallArg }` — FB call with named args
   - `type CallArg struct { NodeBase; Name *Ident; Value Expr; IsOutput bool }` — named parameter (`:=` or `=>`)
   - `type IfStmt struct { NodeBase; Condition Expr; Then []Statement; ElsIfs []*ElsIf; Else []Statement }` — per PARS-07
   - `type ElsIf struct { NodeBase; Condition Expr; Body []Statement }`
   - `type CaseStmt struct { NodeBase; Expr Expr; Branches []*CaseBranch; ElseBranch []Statement }` — per PARS-07
   - `type CaseBranch struct { NodeBase; Labels []CaseLabel; Body []Statement }`
   - `type CaseLabel interface { Node; caseLabelNode() }` — single value, range, or list
   - `type CaseLabelValue struct { NodeBase; Value Expr }` — single value
   - `type CaseLabelRange struct { NodeBase; Low Expr; High Expr }` — `1..10`
   - `type ForStmt struct { NodeBase; Variable *Ident; From Expr; To Expr; By Expr; Body []Statement }` — per PARS-07
   - `type WhileStmt struct { NodeBase; Condition Expr; Body []Statement }` — per PARS-07
   - `type RepeatStmt struct { NodeBase; Body []Statement; Condition Expr }` — per PARS-07 (REPEAT...UNTIL)
   - `type ReturnStmt struct { NodeBase }`
   - `type ExitStmt struct { NodeBase }`
   - `type ContinueStmt struct { NodeBase }`
   - `type EmptyStmt struct { NodeBase }` — bare semicolons
   - All implement `Statement` interface (stmtNode()) and `Children() []Node`

3. Create `pkg/ast/expr.go` — ALL expression node types:
   - `type BinaryExpr struct { NodeBase; Left Expr; Op Token; Right Expr }` — all binary ops (per PARS-07 precedence)
   - `type UnaryExpr struct { NodeBase; Op Token; Operand Expr }` — NOT, unary minus
   - `type Literal struct { NodeBase; Kind LiteralKind; Value string; TypePrefix string }` — all literal types
   - `type LiteralKind int` with constants: `LitInt`, `LitReal`, `LitString`, `LitWString`, `LitBool`, `LitTime`, `LitDate`, `LitDateTime`, `LitTod`, `LitTyped` — per PARS-09
   - `type CallExpr struct { NodeBase; Callee Expr; Args []Expr }` — function call in expression position
   - `type MemberAccessExpr struct { NodeBase; Object Expr; Member *Ident }` — dot access
   - `type IndexExpr struct { NodeBase; Object Expr; Indices []Expr }` — array indexing, supports multi-dim
   - `type DerefExpr struct { NodeBase; Operand Expr }` — `^` dereference per PARS-03
   - `type ParenExpr struct { NodeBase; Inner Expr }` — parenthesized expression
   - `type Token struct { Kind int; Text string; Span Span }` — lightweight token reference for operators in AST
   - All implement `Expr` interface (exprNode()) and `Children() []Node`

4. Create `pkg/ast/types.go` — ALL type specifier nodes:
   - `type NamedType struct { NodeBase; Name *Ident }` — simple type reference like `BOOL`, `INT`, `FB_Motor`
   - `type ArrayType struct { NodeBase; Ranges []*SubrangeSpec; ElementType TypeSpec }` — per PARS-09
   - `type SubrangeSpec struct { NodeBase; Low Expr; High Expr }` — array dimension range
   - `type PointerType struct { NodeBase; BaseType TypeSpec }` — `POINTER TO x` per PARS-03
   - `type ReferenceType struct { NodeBase; BaseType TypeSpec }` — `REFERENCE TO x` per PARS-03
   - `type StringType struct { NodeBase; IsWide bool; Length Expr }` — `STRING(80)` or `WSTRING(255)`
   - `type SubrangeType struct { NodeBase; BaseType TypeSpec; Low Expr; High Expr }` — `INT(0..100)` per PARS-09
   - `type EnumType struct { NodeBase; BaseType TypeSpec; Values []*EnumValue }` — per PARS-09
   - `type EnumValue struct { NodeBase; Name *Ident; Value Expr }` — enum member with optional init value
   - `type StructType struct { NodeBase; Members []*StructMember }` — per PARS-09
   - `type StructMember struct { NodeBase; Name *Ident; Type TypeSpec; InitValue Expr }`
   - All implement `TypeSpec` interface (typeSpecNode()) and `Children() []Node`

5. Create `pkg/ast/var.go` — Variable declaration nodes:
   - `type VarSection int` with constants: `VarLocal`, `VarInput`, `VarOutput`, `VarInOut`, `VarTemp`, `VarGlobal`, `VarAccess`, `VarExternal`, `VarConfig` — per PARS-08
   - `func (v VarSection) String() string` returning "VAR", "VAR_INPUT", etc.
   - `type VarBlock struct { NodeBase; Section VarSection; IsConstant bool; IsRetain bool; IsPersistent bool; Declarations []*VarDecl }` — per PARS-08
   - `type VarDecl struct { NodeBase; Names []*Ident; Type TypeSpec; InitValue Expr; AtAddress *Ident }` — `x, y : INT := 0;` or `x AT %IW0 : INT;`
   - `type PragmaNode struct { NodeBase; Text string }` — raw pragma text per PARS-10
   - All implement `Children() []Node`

6. Create `pkg/ast/json.go`:
   - Custom `MarshalJSON` for `Node` interface dispatch — marshals `kind` field as string plus type-specific fields
   - Use `encoding/json` with map construction for polymorphic nodes
   - Ensure `Declaration`, `Statement`, `Expr`, `TypeSpec` interface values marshal with their `kind` discriminator
   - `func MarshalNode(n Node) ([]byte, error)` — top-level helper

7. Create `pkg/ast/json_test.go`:
   - TestMarshalSourceFile: create a SourceFile with a ProgramDecl containing one VarBlock and one AssignStmt, marshal to JSON, verify:
     - Top-level has `"kind":"SourceFile"`
     - Declarations array has one element with `"kind":"ProgramDecl"`
     - VarBlock has `"section":"VAR"`
   - TestMarshalExpr: create BinaryExpr(Literal(1) + Literal(2)), marshal, verify `"kind":"BinaryExpr"`, `"left"` and `"right"` present
   - TestMarshalOOP: create FunctionBlockDecl with Extends, Implements, MethodDecl; verify all JSON fields present
   - TestMarshalTypes: create ArrayType, PointerType, StructType, EnumType; verify all marshal correctly
   - All tests use `require.NoError` for marshal calls and `require.Contains` or JSON path checking on output
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/ast/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pkg/ast/decl.go contains `type SourceFile struct`
    - pkg/ast/decl.go contains `type ProgramDecl struct`
    - pkg/ast/decl.go contains `type FunctionBlockDecl struct`
    - pkg/ast/decl.go contains `type FunctionDecl struct`
    - pkg/ast/decl.go contains `type InterfaceDecl struct`
    - pkg/ast/decl.go contains `type MethodDecl struct`
    - pkg/ast/decl.go contains `type PropertyDecl struct`
    - pkg/ast/decl.go contains `type TypeDecl struct`
    - pkg/ast/decl.go contains `Extends *Ident`
    - pkg/ast/decl.go contains `Implements []*Ident`
    - pkg/ast/stmt.go contains `type IfStmt struct`
    - pkg/ast/stmt.go contains `type CaseStmt struct`
    - pkg/ast/stmt.go contains `type ForStmt struct`
    - pkg/ast/stmt.go contains `type WhileStmt struct`
    - pkg/ast/stmt.go contains `type RepeatStmt struct`
    - pkg/ast/expr.go contains `type BinaryExpr struct`
    - pkg/ast/expr.go contains `type CallExpr struct`
    - pkg/ast/expr.go contains `type DerefExpr struct`
    - pkg/ast/expr.go contains `type LiteralKind int`
    - pkg/ast/types.go contains `type PointerType struct`
    - pkg/ast/types.go contains `type ReferenceType struct`
    - pkg/ast/types.go contains `type ArrayType struct`
    - pkg/ast/types.go contains `type EnumType struct`
    - pkg/ast/types.go contains `type StructType struct`
    - pkg/ast/types.go contains `type SubrangeType struct`
    - pkg/ast/var.go contains `type VarBlock struct`
    - pkg/ast/var.go contains `VarInput`
    - pkg/ast/var.go contains `VarOutput`
    - pkg/ast/var.go contains `VarInOut`
    - pkg/ast/var.go contains `VarTemp`
    - pkg/ast/var.go contains `VarGlobal`
    - pkg/ast/var.go contains `type PragmaNode struct`
    - pkg/ast/json.go contains `MarshalJSON`
    - `go test ./pkg/ast/... -count=1` exits 0
  </acceptance_criteria>
  <done>Complete AST package with all node types for IEC 61131-3 Ed.3 + CODESYS OOP, JSON marshaling, and passing tests</done>
</task>

</tasks>

<verification>
- `go build ./pkg/ast/...` succeeds
- `go test ./pkg/ast/... -v -count=1` passes all JSON round-trip tests
- Every IEC POU type (PROGRAM, FUNCTION_BLOCK, FUNCTION, TYPE, INTERFACE) has a corresponding node type
- CODESYS OOP (METHOD, PROPERTY, EXTENDS, IMPLEMENTS) present in FunctionBlockDecl and InterfaceDecl
- POINTER TO, REFERENCE TO have dedicated TypeSpec types
- All VAR sections (VAR, VAR_INPUT, VAR_OUTPUT, VAR_IN_OUT, VAR_TEMP, VAR_GLOBAL) represented
</verification>

<success_criteria>
- Complete pkg/ast/ package with 30+ node types covering all IEC 61131-3 + CODESYS constructs
- JSON marshaling produces discriminated output with "kind" field on every node
- Trivia attachment (LeadingTrivia, TrailingTrivia) on every node via NodeBase
- Visitor pattern (Walk, Inspect) functional
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-bootstrap-parser/01-02-SUMMARY.md`
</output>
