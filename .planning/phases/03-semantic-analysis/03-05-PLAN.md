---
phase: 03-semantic-analysis
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - pkg/analyzer/analyzer.go
  - pkg/analyzer/analyzer_test.go
  - pkg/analyzer/testdata/multi_file_a.st
  - pkg/analyzer/testdata/multi_file_b.st
  - pkg/analyzer/testdata/vendor_test.st
  - cmd/stc/check.go
  - cmd/stc/check_test.go
  - cmd/stc/stubs.go
autonomous: true
requirements: [SEMA-05, SEMA-06]

must_haves:
  truths:
    - "Analyzer.Analyze() orchestrates: parse -> pass 1 (declarations) -> pass 2 (type check) -> usage check -> vendor check"
    - "Cross-file symbol resolution works: FB declared in file A, used in file B, no error"
    - "stc check <files...> prints diagnostics in file:line:col: severity: message format"
    - "stc check --format json outputs JSON array of diagnostics"
    - "stc check --vendor beckhoff enables vendor-aware warnings"
    - "stc check returns exit code 1 when errors exist, 0 when only warnings or clean"
  artifacts:
    - path: "pkg/analyzer/analyzer.go"
      provides: "Public Analyze() facade orchestrating all passes"
      exports: ["Analyze", "AnalysisResult"]
    - path: "cmd/stc/check.go"
      provides: "stc check CLI command replacing stub"
      exports: ["newCheckCmd"]
  key_links:
    - from: "pkg/analyzer/analyzer.go"
      to: "pkg/checker/resolve.go"
      via: "Calls CollectDeclarations for pass 1"
      pattern: "resolver\\.CollectDeclarations"
    - from: "pkg/analyzer/analyzer.go"
      to: "pkg/checker/check.go"
      via: "Calls CheckBodies for pass 2"
      pattern: "checker\\.CheckBodies"
    - from: "pkg/analyzer/analyzer.go"
      to: "pkg/checker/vendor.go"
      via: "Calls CheckVendorCompat if vendor configured"
      pattern: "checker\\.CheckVendorCompat"
    - from: "pkg/analyzer/analyzer.go"
      to: "pkg/checker/usage.go"
      via: "Calls CheckUsage for unused/unreachable"
      pattern: "checker\\.CheckUsage"
    - from: "cmd/stc/check.go"
      to: "pkg/analyzer/analyzer.go"
      via: "CLI calls Analyze then formats output"
      pattern: "analyzer\\.Analyze"
---

<objective>
Build the analyzer facade and CLI check command, wiring together all checker passes and providing the user-facing `stc check` command with text and JSON output.

Purpose: This is the integration layer that makes the type checker usable. The analyzer facade sequences all passes correctly, and the CLI command gives users the `stc check <files...>` experience promised by the roadmap.
Output: `pkg/analyzer/` package and `cmd/stc/check.go` replacing the stub.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-analysis/03-RESEARCH.md
@.planning/phases/03-semantic-analysis/03-03-SUMMARY.md
@.planning/phases/03-semantic-analysis/03-04-SUMMARY.md

@pkg/diag/diagnostic.go
@pkg/diag/collector.go
@pkg/project/config.go
@cmd/stc/stubs.go

<interfaces>
<!-- From pkg/checker/ (Plans 03+04) -->
Resolver: NewResolver(table, diags), CollectDeclarations(files)
Checker: NewChecker(table, diags), CheckBodies(files)
CheckVendorCompat(files, table, profile, diags)
CheckUsage(files, table, diags)
LookupVendor(name string) *VendorProfile

<!-- From pkg/symbols/ (Plan 02) -->
Table: NewTable()

<!-- From pkg/parser/ (existing) -->
parser.ParseFile(filename, source string) (*ast.SourceFile, []diag.Diagnostic)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyzer facade — Analyze() orchestrating all checker passes</name>
  <files>pkg/analyzer/analyzer.go, pkg/analyzer/analyzer_test.go, pkg/analyzer/testdata/multi_file_a.st, pkg/analyzer/testdata/multi_file_b.st, pkg/analyzer/testdata/vendor_test.st</files>
  <read_first>
    - .planning/phases/03-semantic-analysis/03-RESEARCH.md (Analyzer Public API section)
    - .planning/phases/03-semantic-analysis/03-03-SUMMARY.md (checker pass 1 + pass 2 APIs)
    - .planning/phases/03-semantic-analysis/03-04-SUMMARY.md (vendor + usage checker APIs)
    - pkg/project/config.go (Config struct, Build.VendorTarget, Build.SourceRoots)
    - pkg/diag/collector.go (Collector API)
  </read_first>
  <action>
    Create `pkg/analyzer/analyzer.go`:
    - AnalysisResult struct: Symbols *symbols.Table, Diags []diag.Diagnostic
    - Analyze(files []*ast.SourceFile, cfg *project.Config) AnalysisResult — per RESEARCH.md:
      1. Create symbols.NewTable() and diag.NewCollector()
      2. Pass 1: checker.NewResolver(table, diags).CollectDeclarations(files)
      3. Pass 2: checker.NewChecker(table, diags).CheckBodies(files)
      4. Usage check: checker.CheckUsage(files, table, diags)
      5. Vendor check (optional): if cfg != nil and cfg.Build.VendorTarget != "", look up profile via checker.LookupVendor, if found call checker.CheckVendorCompat
      6. Return AnalysisResult{Symbols: table, Diags: diags.All()}
    - AnalyzeFiles(filenames []string, cfg *project.Config) AnalysisResult — convenience that reads files from disk, parses each with parser.ParseFile, collects parse diagnostics, then calls Analyze. If stc.toml source_roots are configured and filenames is empty, discover .st files from source roots.

    Create test fixtures:
    - testdata/multi_file_a.st: FUNCTION_BLOCK FB_Motor with VAR_INPUT bStart : BOOL, method Run
    - testdata/multi_file_b.st: PROGRAM Main with VAR motor : FB_Motor; body calls motor(bStart := TRUE);

    Create `pkg/analyzer/analyzer_test.go`:
    - TestAnalyzeSingleFile: Parse a valid single-file program, Analyze returns 0 errors
    - TestAnalyzeCrossFile: Parse multi_file_a.st and multi_file_b.st, Analyze resolves FB_Motor in file B using declaration from file A. No undeclared errors. (SEMA-05 cross-file resolution)
    - TestAnalyzeWithTypeMismatch: Parse file with type error, Analyze returns SEMA001 diagnostic with correct position
    - TestAnalyzeWithVendor: Parse vendor_test.st (using METHOD), Analyze with schneider config returns VEND001 warning
    - TestAnalyzeNilConfig: Analyze with nil config works (defaults to no vendor checks)
    - TestAnalyzeParseErrors: AnalyzeFiles with broken ST returns parse errors in diagnostics
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/analyzer/... -count=1 -v</automated>
  </verify>
  <done>Analyzer facade correctly sequences all passes, cross-file resolution works (FB in file A used in file B), vendor checks triggered by config, parse errors included in output.</done>
</task>

<task type="auto">
  <name>Task 2: CLI check command replacing stub with real implementation</name>
  <files>cmd/stc/check.go, cmd/stc/stubs.go, cmd/stc/check_test.go</files>
  <read_first>
    - cmd/stc/stubs.go (current stub to replace)
    - pkg/analyzer/analyzer.go (just created — AnalyzeFiles API)
    - pkg/project/config.go (FindConfig, LoadConfig for stc.toml discovery)
    - pkg/diag/diagnostic.go (Diagnostic.String() format)
  </read_first>
  <action>
    Create `cmd/stc/check.go`:
    - newCheckCmd() *cobra.Command — replaces stub in stubs.go
    - Usage: `stc check <files...> [--vendor beckhoff|schneider|portable] [--format text|json]`
    - Implementation:
      1. Collect file arguments from args
      2. Try FindConfig from current directory for stc.toml. If found, load it. If --vendor flag provided, override config's VendorTarget.
      3. Call analyzer.AnalyzeFiles(files, config)
      4. Format output based on --format flag:
         - text (default): Print each diagnostic via Diagnostic.String() to stderr. Print summary line "N error(s), M warning(s)" to stderr.
         - json: JSON marshal result.Diags array to stdout (same pattern as parse command)
      5. Exit code: if any Error-severity diagnostics, os.Exit(1). Warnings alone = exit 0.
    - Add --vendor flag (string, optional)

    Modify `cmd/stc/stubs.go`:
    - Remove the newCheckCmd() stub function (it's now in check.go)

    Create `cmd/stc/check_test.go`:
    - TestCheckCommandValid: Create temp .st file with valid PROGRAM, run stc check, expect exit 0 and no errors in output
    - TestCheckCommandTypeError: Create temp .st file with type mismatch, run stc check, expect exit 1 and SEMA001 in output
    - TestCheckCommandJSON: Run with --format json, expect valid JSON array with diagnostic objects having severity/pos/code/message fields
    - TestCheckCommandVendor: Run with --vendor schneider on file using METHOD, expect VEND001 in output, exit 0 (warnings don't cause exit 1)
    - TestCheckCommandNoFiles: Run with no arguments, expect helpful error message
    - Use exec.Command pattern from existing integration tests (Phase 01 pattern)
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./cmd/stc/... -count=1 -run TestCheck -v && go build ./cmd/stc/ && echo "Build OK"</automated>
  </verify>
  <done>stc check command works end-to-end: reads .st files, runs full semantic analysis, outputs diagnostics in text/JSON format, returns correct exit code. Stub replaced with real implementation.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/analyzer/... -count=1` passes
- `go test ./cmd/stc/... -count=1` passes
- `go build ./cmd/stc/` succeeds
- `go test ./... -count=1` — full suite green
- Manual: `./stc check testfile.st` outputs diagnostics
- Manual: `./stc check testfile.st --format json` outputs JSON
- Manual: `./stc check testfile.st --vendor schneider` shows vendor warnings
</verification>

<success_criteria>
- Cross-file symbol resolution works (SEMA-05): FB declared in one file, used in another
- stc check outputs text diagnostics to stderr with file:line:col format (SEMA-06)
- stc check --format json outputs machine-readable diagnostics (SEMA-06)
- stc check --vendor flag triggers vendor-aware warnings
- Exit code 1 on errors, 0 on warnings-only or clean
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-analysis/03-05-SUMMARY.md`
</output>
