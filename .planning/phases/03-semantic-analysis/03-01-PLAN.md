---
phase: 03-semantic-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/types/types.go
  - pkg/types/lattice.go
  - pkg/types/builtin.go
  - pkg/types/types_test.go
  - pkg/types/lattice_test.go
autonomous: true
requirements: [SEMA-02]

must_haves:
  truths:
    - "All 20+ IEC elementary types are represented as distinct type constants"
    - "Implicit widening follows IEC rules exactly: SINT->INT->DINT->LINT, REAL->LREAL, BYTE->WORD->DWORD->LWORD"
    - "Cross-category widening INT->REAL works for precision-preserving cases only"
    - "Signed-to-unsigned, BIT-to-INT, and BOOL-to-anything conversions are rejected"
    - "CommonType finds the smallest common supertype for two compatible types"
    - "Array, Struct, Enum, FunctionBlock, Function, Pointer, Reference types are representable"
  artifacts:
    - path: "pkg/types/types.go"
      provides: "Type interface, TypeKind enum, concrete type structs"
      exports: ["Type", "TypeKind", "PrimitiveType", "ArrayType", "StructType", "EnumType", "FunctionBlockType", "FunctionType", "PointerType", "ReferenceType"]
    - path: "pkg/types/lattice.go"
      provides: "Widening rules, CommonType, CanWiden, category membership"
      exports: ["CanWiden", "CommonType", "IsAnyInt", "IsAnyReal", "IsAnyNum", "IsAnyBit"]
    - path: "pkg/types/builtin.go"
      provides: "Built-in type constants and standard function signatures"
      exports: ["TypeBOOL", "TypeINT", "TypeDINT", "TypeREAL", "BuiltinFunctions"]
  key_links:
    - from: "pkg/types/lattice.go"
      to: "pkg/types/types.go"
      via: "TypeKind constants"
      pattern: "Kind[A-Z]+"
---

<objective>
Build the IEC 61131-3 type system package with type representations, widening lattice, and built-in type/function constants.

Purpose: This is the foundation all type checking depends on. The type lattice encodes the IEC standard's implicit conversion rules as auditable data tables rather than if/else chains.
Output: `pkg/types/` package with complete type system, fully tested.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-analysis/03-RESEARCH.md

@pkg/ast/node.go
@pkg/ast/types.go
@pkg/ast/expr.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type system core — Type interface, TypeKind enum, concrete types, and widening lattice</name>
  <files>pkg/types/types.go, pkg/types/lattice.go, pkg/types/types_test.go, pkg/types/lattice_test.go</files>
  <read_first>
    - pkg/ast/node.go (NodeKind pattern to follow for TypeKind)
    - pkg/ast/types.go (AST type spec nodes the checker will map from)
    - .planning/phases/03-semantic-analysis/03-RESEARCH.md (IEC type hierarchy, widening rules, lattice pattern)
  </read_first>
  <behavior>
    - TestPrimitiveTypeKinds: Each IEC elementary type (BOOL, BYTE, WORD, DWORD, LWORD, SINT, INT, DINT, LINT, USINT, UINT, UDINT, ULINT, REAL, LREAL, STRING, WSTRING, TIME, DATE, DT, TOD, CHAR, WCHAR) has a distinct TypeKind constant and String() returns its name
    - TestTypeInterface: PrimitiveType, ArrayType, StructType, EnumType, FunctionBlockType, FunctionType, PointerType, ReferenceType all implement Type interface
    - TestCanWiden_SignedInts: SINT->INT true, INT->DINT true, DINT->LINT true, LINT->SINT false, INT->SINT false
    - TestCanWiden_UnsignedInts: USINT->UINT true, UINT->UDINT true, UDINT->ULINT true, ULINT->USINT false
    - TestCanWiden_Reals: REAL->LREAL true, LREAL->REAL false
    - TestCanWiden_CrossCategory: INT->REAL true, DINT->LREAL true, LINT->LREAL false (precision loss)
    - TestCanWiden_BitTypes: BYTE->WORD true, WORD->DWORD true, DWORD->LWORD true
    - TestCanWiden_Rejected: SINT->USINT false, BYTE->INT false, BOOL->INT false, STRING->INT false, TIME->INT false
    - TestCommonType_Same: CommonType(INT, INT) = INT
    - TestCommonType_Widening: CommonType(INT, DINT) = DINT, CommonType(SINT, LINT) = LINT
    - TestCommonType_Incompatible: CommonType(INT, STRING) returns false
    - TestCategoryMembership: IsAnyInt covers SINT/INT/DINT/LINT/USINT/UINT/UDINT/ULINT; IsAnyReal covers REAL/LREAL; IsAnyBit covers BOOL/BYTE/WORD/DWORD/LWORD; IsAnyNum covers IsAnyInt + IsAnyReal
    - TestInvalidType: Invalid type kind for error propagation
  </behavior>
  <action>
    Create `pkg/types/types.go`:
    - Type interface with methods: Kind() TypeKind, String() string, Equal(Type) bool
    - TypeKind int enum with constants for all IEC elementary types (KindBOOL through KindTOD), plus KindInvalid for error propagation, KindVoid for procedures without return
    - Concrete types: PrimitiveType (wraps TypeKind), ArrayType (ElementType Type, Dimensions []ArrayDimension), StructType (Name string, Members []StructMember), EnumType (Name string, BaseType TypeKind, Values []string), FunctionBlockType (Name string, Inputs/Outputs/InOuts []Parameter), FunctionType (Name string, ReturnType Type, Params []Parameter), PointerType (BaseType Type), ReferenceType (BaseType Type)
    - Parameter struct: Name string, Type Type, Direction ParamDirection (Input/Output/InOut)
    - ArrayDimension struct: Low, High int
    - StructMember struct: Name string, Type Type
    - Invalid sentinel: var Invalid Type = PrimitiveType{KindInvalid}

    Create `pkg/types/lattice.go`:
    - wideningRules map[TypeKind][]TypeKind with IEC-only rules per RESEARCH.md (no CODESYS permissive). Per user decision: strict IEC widening only.
    - CanWiden(from, to TypeKind) bool — lookup in wideningRules
    - CommonType(a, b TypeKind) (TypeKind, bool) — if same return same; if one widens to other return wider; else search for smallest common supertype in widening graph
    - Category membership functions using bitmask or set: IsAnyInt, IsAnySigned, IsAnyUnsigned, IsAnyReal, IsAnyNum, IsAnyBit, IsAnyString, IsAnyDate, IsAnyChar
    - Note: BOOL is in ANY_BIT per IEC but NOT implicitly convertible to BYTE. Track category membership separately from widening.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/types/... -count=1 -v</automated>
  </verify>
  <done>All IEC primitive types represented, widening rules match IEC standard exactly, category membership functions correct, CommonType works for all compatible pairs and rejects incompatible ones</done>
</task>

<task type="auto">
  <name>Task 2: Built-in type constants and standard function signatures</name>
  <files>pkg/types/builtin.go</files>
  <read_first>
    - pkg/types/types.go (just created — Type interface, TypeKind, FunctionType)
    - .planning/phases/03-semantic-analysis/03-RESEARCH.md (standard function signatures section, Open Question 2)
  </read_first>
  <action>
    Create `pkg/types/builtin.go`:
    - Package-level Type constants for all elementary types: TypeBOOL, TypeBYTE, TypeWORD, TypeDWORD, TypeLWORD, TypeSINT, TypeINT, TypeDINT, TypeLINT, TypeUSINT, TypeUINT, TypeUDINT, TypeULINT, TypeREAL, TypeLREAL, TypeSTRING, TypeWSTRING, TypeTIME, TypeDATE, TypeDT, TypeTOD, TypeCHAR, TypeWCHAR
    - LookupElementaryType(name string) (Type, bool) — case-insensitive name-to-type lookup for type resolution (IEC identifiers are case-insensitive per Phase 01 decision)
    - BuiltinFunctions map[string]*FunctionType — registry of the ~20 most-used standard function signatures with ANY_NUM/ANY_INT/ANY_REAL generic parameter types:
      - Arithmetic: ADD, SUB, MUL, DIV, MOD (ANY_NUM -> ANY_NUM)
      - Math: ABS, SQRT (ANY_REAL -> ANY_REAL), MIN, MAX (ANY_NUM, ANY_NUM -> ANY_NUM), LIMIT (ANY_NUM, ANY_NUM, ANY_NUM -> ANY_NUM), SEL (BOOL, ANY, ANY -> ANY), MUX (ANY_INT, ANY... -> ANY)
      - Conversion: *_TO_* pattern — register common ones: INT_TO_REAL, REAL_TO_INT, BOOL_TO_INT, INT_TO_STRING, etc.
      - String: LEN (STRING -> INT), CONCAT (STRING, STRING -> STRING), LEFT, RIGHT, MID, FIND
      - MOVE (ANY -> ANY)
    - For functions with ANY_* parameters, store the category constraint (IsAnyNum, IsAnyInt, etc.) so the checker can validate arguments. Use a GenericConstraint field on Parameter.
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/types/... -count=1 -run TestBuiltin</automated>
  </verify>
  <done>All elementary type constants exist, LookupElementaryType resolves all type names case-insensitively, BuiltinFunctions contains ~20 standard function signatures with correct parameter types</done>
</task>

</tasks>

<verification>
- `go test ./pkg/types/... -count=1` passes all tests
- `go vet ./pkg/types/...` reports no issues
- Types package has zero dependencies on other stc packages (self-contained foundation)
</verification>

<success_criteria>
- All 23 IEC elementary types represented with distinct TypeKind constants
- Widening rules encode IEC-only implicit conversions (no CODESYS permissive)
- CommonType correctly finds smallest common supertype
- Category membership functions cover the full ANY hierarchy
- Built-in function registry covers ~20 standard functions
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-analysis/03-01-SUMMARY.md`
</output>
