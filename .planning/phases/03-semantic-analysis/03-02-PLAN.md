---
phase: 03-semantic-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/symbols/symbol.go
  - pkg/symbols/scope.go
  - pkg/symbols/table.go
  - pkg/symbols/scope_test.go
  - pkg/symbols/table_test.go
autonomous: true
requirements: [SEMA-04, SEMA-05]

must_haves:
  truths:
    - "Symbol table uses hierarchical scope chain: Global -> POU -> Method -> Block"
    - "Name lookup is case-insensitive (IEC 61131-3 requirement)"
    - "Original identifier casing is preserved for diagnostic messages"
    - "Symbols track declaration position, type reference, usage count, and parameter direction"
    - "Scope.Lookup walks up the parent chain until found or reaches global scope"
    - "Redeclaration in same scope is detected and reported"
  artifacts:
    - path: "pkg/symbols/symbol.go"
      provides: "Symbol struct, SymbolKind enum, ParamDirection"
      exports: ["Symbol", "SymbolKind", "KindVariable", "KindFunction", "KindFunctionBlock", "KindProgram", "KindType", "KindEnumValue", "KindInterface"]
    - path: "pkg/symbols/scope.go"
      provides: "Scope with parent chain, Insert, Lookup"
      exports: ["Scope", "ScopeKind", "NewScope", "Lookup", "Insert"]
    - path: "pkg/symbols/table.go"
      provides: "SymbolTable facade, POU registry, file tracking"
      exports: ["Table", "NewTable"]
  key_links:
    - from: "pkg/symbols/scope.go"
      to: "pkg/symbols/symbol.go"
      via: "Scope stores *Symbol values"
      pattern: "map\\[string\\]\\*Symbol"
---

<objective>
Build the hierarchical symbol table package with scope chains, symbol definitions, and a table facade for global symbol management.

Purpose: The symbol table is the central data structure for name resolution. It must handle IEC 61131-3's case-insensitive identifiers, hierarchical scoping (global/POU/method/block), and track usage for unused variable detection.
Output: `pkg/symbols/` package with scope chain and symbol table, fully tested.
</objective>

<execution_context>
@/Users/jonb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-semantic-analysis/03-RESEARCH.md

@pkg/source/source.go
@pkg/ast/node.go
@pkg/ast/var.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Symbol type and Scope chain with case-insensitive lookup</name>
  <files>pkg/symbols/symbol.go, pkg/symbols/scope.go, pkg/symbols/scope_test.go</files>
  <read_first>
    - pkg/source/source.go (Pos and Span types used for symbol positions)
    - pkg/ast/var.go (VarSection enum — maps to parameter direction)
    - .planning/phases/03-semantic-analysis/03-RESEARCH.md (Pattern 1: Hierarchical Scope Chain, Pitfall 2: case-insensitive identifiers)
  </read_first>
  <behavior>
    - TestScopeInsertAndLookup: Insert symbol "counter" into scope, Lookup("counter") returns it, Lookup("COUNTER") returns same symbol, Lookup("Counter") returns same symbol
    - TestScopeParentLookup: Insert "x" in parent scope, create child scope, child.Lookup("x") finds it in parent
    - TestScopeShadowing: Insert "x" in parent, insert "x" in child, child.Lookup("x") returns child's symbol (not parent's)
    - TestScopeRedeclaration: Insert "x" twice in same scope returns error with original declaration position
    - TestScopeNotFound: Lookup("nonexistent") in scope chain returns nil
    - TestSymbolKinds: KindVariable, KindFunction, KindFunctionBlock, KindProgram, KindType, KindEnumValue, KindInterface all have distinct values and String() returns names
    - TestScopeKinds: ScopeGlobal, ScopePOU, ScopeMethod, ScopeBlock all have distinct values
    - TestSymbolUsageTracking: Symbol.Used starts false, calling MarkUsed() sets it to true
    - TestScopeChildren: Child scopes are tracked in parent's Children slice
    - TestCasePreservation: Insert symbol with name "myVar", Lookup("MYVAR") returns symbol with original Name == "myVar"
  </behavior>
  <action>
    Create `pkg/symbols/symbol.go`:
    - SymbolKind int enum: KindVariable, KindFunction, KindFunctionBlock, KindProgram, KindType, KindEnumValue, KindInterface, KindMethod, KindProperty
    - Symbol struct: Name string (original casing), Kind SymbolKind, Pos source.Pos (declaration site), Span source.Span (full declaration span), Used bool (for unused detection), ParamDir ast.VarSection (for VAR_IN_OUT checking). Type field will be added as `any` initially — typed to types.Type once Plan 01 is available (both are Wave 1, no circular import issue since symbols imports types).
    - Actually: to avoid import cycle, store Type as `any` interface field. The checker will type-assert. Alternative: use a forward-declared interface. Best approach: `pkg/symbols` should NOT import `pkg/types`. Store type info as `any`. The checker bridges both.
    - MarkUsed() method sets Used = true
    - String() returns "SymbolKind name at file:line:col"

    Create `pkg/symbols/scope.go`:
    - ScopeKind int enum: ScopeGlobal, ScopePOU, ScopeMethod, ScopeBlock
    - Scope struct: Parent *Scope, Kind ScopeKind, Name string (POU/method name), Children []*Scope, symbols map[string]*Symbol (keyed by UPPERCASE name)
    - NewScope(parent *Scope, kind ScopeKind, name string) *Scope
    - Insert(sym *Symbol) error — stores under strings.ToUpper(sym.Name), returns error if already exists with position of first declaration
    - Lookup(name string) *Symbol — case-insensitive lookup walking parent chain
    - LookupLocal(name string) *Symbol — case-insensitive lookup in THIS scope only (no parent walk)
    - Symbols() []*Symbol — returns all symbols in this scope (for unused detection iteration)
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/symbols/... -count=1 -v</automated>
  </verify>
  <done>Scope chain supports hierarchical lookup, case-insensitive matching, shadowing, redeclaration detection, and usage tracking. Original casing preserved in Symbol.Name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SymbolTable facade with POU registry and file tracking</name>
  <files>pkg/symbols/table.go, pkg/symbols/table_test.go</files>
  <read_first>
    - pkg/symbols/scope.go (just created — Scope, NewScope)
    - pkg/symbols/symbol.go (just created — Symbol, SymbolKind)
  </read_first>
  <behavior>
    - TestTableNewHasGlobalScope: NewTable() creates a table with a global scope of kind ScopeGlobal
    - TestTableRegisterPOU: Register a POU "MyProgram" with KindProgram, table.LookupPOU("MYPROGRAM") returns it (case-insensitive)
    - TestTableEnterExitScope: EnterScope(ScopePOU, "FB_Motor") pushes scope, CurrentScope() returns POU scope, ExitScope() pops back to global
    - TestTableNestedScopes: Enter POU, enter Method, lookup walks method->POU->global
    - TestTableGlobalLookup: Insert symbol in global scope, LookupGlobal("name") finds it
    - TestTableFileTracking: RegisterFile("main.st"), Files() returns ["main.st"]
  </behavior>
  <action>
    Create `pkg/symbols/table.go`:
    - Table struct: global *Scope (root), current *Scope (current position in scope tree), pous map[string]*Scope (POU name -> POU scope, uppercased keys), files []string
    - NewTable() *Table — creates table with global scope, sets current = global
    - GlobalScope() *Scope — returns global scope
    - CurrentScope() *Scope — returns current scope
    - EnterScope(kind ScopeKind, name string) *Scope — creates child of current, sets current to child, returns child
    - ExitScope() — sets current = current.Parent (panics if at global — programming error)
    - RegisterPOU(name string, kind SymbolKind) *Scope — inserts POU symbol in global scope, creates POU scope, registers in pous map
    - LookupPOU(name string) *Scope — case-insensitive lookup in pous map
    - LookupGlobal(name string) *Symbol — lookup in global scope only
    - RegisterFile(filename string) — tracks analyzed files
    - Files() []string — returns tracked files
    - Insert(sym *Symbol) error — inserts into current scope (convenience method)
    - Lookup(name string) *Symbol — lookups in current scope chain (convenience method)
  </action>
  <verify>
    <automated>cd /Users/jonb/Projects/stc && go test ./pkg/symbols/... -count=1 -v</automated>
  </verify>
  <done>SymbolTable provides complete scope management with POU registry, file tracking, and convenience methods for the checker to use.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/symbols/... -count=1` passes all tests
- `go vet ./pkg/symbols/...` reports no issues
- Symbols package only imports source package (no circular dependencies)
</verification>

<success_criteria>
- Hierarchical scope chain with Global/POU/Method/Block levels
- Case-insensitive lookup with original casing preserved
- Redeclaration detection with source positions
- Usage tracking for unused variable detection
- SymbolTable facade manages scope stack and POU registry
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-analysis/03-02-SUMMARY.md`
</output>
